mixin scholl-icons(icon, name, title)
    a.scholl-icons(href="#")
        .scholl-icons__icons
            .scholl-icons__icons_icon
                +svg(icon)
            h3!= name
            p!= title
